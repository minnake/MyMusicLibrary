<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Shop_buttons" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_height">70dp</item>
        <item name="android:layout_width">70dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <style name="Shop_text_view" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

    <style name="list_style" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_height">380dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:scrollbarAlwaysDrawHorizontalTrack">true</item>
        <item name="android:drawSelectorOnTop">true</item>
    </style>

    <style name="player_button_style" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_height">30dp</item>
        <item name="android:layout_width">30dp</item>
    </style>

    <style name="play_button_style" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_width">60dp</item>
    </style>

</resources>